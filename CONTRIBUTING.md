# Contributing

```bash
git clone https://github.com/wadezigh96/STIVIUM
cd STIVIUM
# open index.html — no install
```

Keep `index.html` runnable without a build step for the core demo.

If you change rarity or trending math, update the formulas in `README.md` and `docs/JUDGING.md`.

Useful directions:
- live data from Agent Studio / 8004scan
- real Altana session keys on activate
- real runs in the TermiX advantage report
- more agents or richer category metrics

PRs: don't commit secrets, keep all four categories working.
